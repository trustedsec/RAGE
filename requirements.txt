# RAGE validator dependencies. Install into a local venv:
#   python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
jsonschema>=4.0    # full JSON Schema validation of records (validate/rage_validate.py)
PyYAML>=6.0        # rule-corpus validation (validate/check_rules.py)
